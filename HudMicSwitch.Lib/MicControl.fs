namespace HudMicSwitch.Lib

module MicControl =
    let hello = "hello"
//        let deviceEnumerator = new MMDeviceEnumerator()
//        let devices = deviceEnumerator.EnumerateAudioEndPoints( DataFlow.All, DeviceState.Active)
//        match devices with
//        | null -> List<MMDevice>
//        | some -> some.ToList()
